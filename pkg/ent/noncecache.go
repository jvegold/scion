// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/GoogleCloudPlatform/scion/pkg/ent/noncecache"
	"github.com/google/uuid"
)

// NonceCache is the model entity for the NonceCache schema.
type NonceCache struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The HMAC request nonce value
	Nonce string `json:"nonce,omitempty"`
	// When this nonce entry expires and can be purged
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// When this nonce was first seen
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NonceCache) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case noncecache.FieldNonce:
			values[i] = new(sql.NullString)
		case noncecache.FieldExpiresAt, noncecache.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case noncecache.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NonceCache fields.
func (_m *NonceCache) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case noncecache.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case noncecache.FieldNonce:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nonce", values[i])
			} else if value.Valid {
				_m.Nonce = value.String
			}
		case noncecache.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case noncecache.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NonceCache.
// This includes values selected through modifiers, order, etc.
func (_m *NonceCache) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this NonceCache.
// Note that you need to call NonceCache.Unwrap() before calling this method if this NonceCache
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *NonceCache) Update() *NonceCacheUpdateOne {
	return NewNonceCacheClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the NonceCache entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *NonceCache) Unwrap() *NonceCache {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: NonceCache is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *NonceCache) String() string {
	var builder strings.Builder
	builder.WriteString("NonceCache(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("nonce=")
	builder.WriteString(_m.Nonce)
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NonceCaches is a parsable slice of NonceCache.
type NonceCaches []*NonceCache
