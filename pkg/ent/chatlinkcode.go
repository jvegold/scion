// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/GoogleCloudPlatform/scion/pkg/ent/chatlinkcode"
	"github.com/google/uuid"
)

// ChatLinkCode is the model entity for the ChatLinkCode schema.
type ChatLinkCode struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SHA-256 hash of the plaintext link code
	CodeHash string `json:"-"`
	// Platform-specific user ID (Telegram user ID, Discord user ID, Teams AAD object ID)
	UserIdentifier string `json:"user_identifier,omitempty"`
	// Chat provider: telegram, discord, or teams
	Provider chatlinkcode.Provider `json:"provider,omitempty"`
	// Link status: pending or confirmed
	Status chatlinkcode.Status `json:"status,omitempty"`
	// Scion user ID, set when a logged-in user confirms the code
	UserID *string `json:"user_id,omitempty"`
	// Scion user email, set when a logged-in user confirms the code
	UserEmail *string `json:"user_email,omitempty"`
	// When this link code expires
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChatLinkCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chatlinkcode.FieldCodeHash, chatlinkcode.FieldUserIdentifier, chatlinkcode.FieldProvider, chatlinkcode.FieldStatus, chatlinkcode.FieldUserID, chatlinkcode.FieldUserEmail:
			values[i] = new(sql.NullString)
		case chatlinkcode.FieldExpiresAt, chatlinkcode.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case chatlinkcode.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChatLinkCode fields.
func (_m *ChatLinkCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chatlinkcode.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case chatlinkcode.FieldCodeHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code_hash", values[i])
			} else if value.Valid {
				_m.CodeHash = value.String
			}
		case chatlinkcode.FieldUserIdentifier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_identifier", values[i])
			} else if value.Valid {
				_m.UserIdentifier = value.String
			}
		case chatlinkcode.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = chatlinkcode.Provider(value.String)
			}
		case chatlinkcode.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = chatlinkcode.Status(value.String)
			}
		case chatlinkcode.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = new(string)
				*_m.UserID = value.String
			}
		case chatlinkcode.FieldUserEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_email", values[i])
			} else if value.Valid {
				_m.UserEmail = new(string)
				*_m.UserEmail = value.String
			}
		case chatlinkcode.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case chatlinkcode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChatLinkCode.
// This includes values selected through modifiers, order, etc.
func (_m *ChatLinkCode) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ChatLinkCode.
// Note that you need to call ChatLinkCode.Unwrap() before calling this method if this ChatLinkCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChatLinkCode) Update() *ChatLinkCodeUpdateOne {
	return NewChatLinkCodeClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChatLinkCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChatLinkCode) Unwrap() *ChatLinkCode {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ChatLinkCode is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChatLinkCode) String() string {
	var builder strings.Builder
	builder.WriteString("ChatLinkCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("code_hash=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("user_identifier=")
	builder.WriteString(_m.UserIdentifier)
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(fmt.Sprintf("%v", _m.Provider))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	if v := _m.UserID; v != nil {
		builder.WriteString("user_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.UserEmail; v != nil {
		builder.WriteString("user_email=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChatLinkCodes is a parsable slice of ChatLinkCode.
type ChatLinkCodes []*ChatLinkCode
